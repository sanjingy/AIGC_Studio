# 超哥短剧 Skill 接入方案（Lead 分析）

> 来源：`E:\AImanju\chaogeskill\chaoge-shortdrama-pipeline`（131 KB，SKILL.md + 6 份 reference）
> 结论先说：**按阶段拆，但不是按它的阶段拆，是按我们的接缝拆。** 它的 13 个技能落到我们这里
> 只有 4 个是 Agent 提示词，5 个是确定性代码，3 个是我们缺的新能力，1 个不要。

---

## 1. 它是什么

一份把三位作者的提示词（小说改编大师 v2.31、角色场景美术指导 v2.1、分镜总导演 v4.3）
合并成的流水线，13 个技能分三层：

| 层 | 技能 | 产出 |
|---|---|---|
| A 剧本 | A1 改编 / A2 洗稿 | 标准短剧剧本 |
| B 美术 | B1 解析 / B2 角色档案 / B3 角色立绘提示词 / B4 场景档案 / B5 场景概念图提示词 / B6 台词SRT / B7 封面 | 档案 + 可直接投喂绘画工具的提示词 |
| C 分镜 | C1 分镜表 / C2 逐镜视频提示词 / C3 批次视频提示词 / C4 九宫格关键帧 | 9 列分镜表 + Seedance/Nano Banana 提示词 |

它的核心机制有三条：**画风只确认一次**（STYLE_ID ①~⑧，自动派发人物/场景/视频三套描述词）、
**全局变量跨阶段贯通**（ERA_TOKEN、TONE_TOKEN、情节目录、角色称呼映射表、空间锚点卡）、
**时长按公式算不许估**。

## 2. 为什么不能直接照搬

我们和它有一条**根本的架构分歧**，而且这条分歧我们是对的：

> **它让模型写提示词，我们让代码写提示词。**

它的 B3（角色立绘提示词）、B5（场景概念图提示词）、C2/C3（视频提示词）、C4（九宫格）
都是"让 LLM 把档案 + 画风词拼成一段提示词"。我们的硬规则第 9 条写着
**「Agent 不许自己写风格词 —— 画风漂移头号来源」**，提示词一律由
`consistency.compose` 这段确定性代码合成。

所以照搬 = 把我们最重要的一道防线拆掉。**正确做法是把它的"知识"搬过来，把它的"执行方式"丢掉。**

它那些提示词模板不是没价值，价值极高——但它们应该变成**我们代码里的模板常量**，
而不是变成 Agent 的自由发挥。

## 3. 逐个技能的去处

| 它的技能 | 去我们哪儿 | 形态 |
|---|---|---|
| A1 / A2 剧本改编·洗稿 | `agents/builtin/story_screenplay.yaml` | **Agent 提示词增强** |
| B1 剧本解析 | 已有（`story_plot_index.yaml` 情节目录 + 覆盖核验），它的更细 | Agent 提示词增强 |
| B2 角色档案 | `agents/builtin/visual_character.yaml` | **Agent 提示词增强 + schema 补字段** |
| B4 场景档案 | `agents/builtin/visual_scene.yaml` | **Agent 提示词增强 + schema 补字段** |
| C1 分镜表 | `agents/builtin/visual_storyboard.yaml` | **Agent 提示词增强** |
| **B3 角色立绘提示词** | `consistency/compose.py::compose_character` | **确定性代码，不是 Agent** |
| **B5 场景概念图提示词** | `consistency/compose.py::describe_scene` | **确定性代码** |
| **C2 逐镜视频提示词** | `compose.py` 新增 `compose_shot_video`（M2） | **确定性代码** |
| **画风词库 ①~⑧** | `style_profiles` 表 | **数据，不是提示词** |
| **时长引擎** | 分镜阶段的确定性计算 | **新能力，见 §5** |
| **空间锚点卡** | `SceneSheet.camera_axis` / `fixed_references`（**字段已存在**） | 填上我们空着的字段 |
| C4 九宫格关键帧 | 待评估，见 §6 | 新能力 |
| C3 批次版（多镜合成一次生成） | **与 ADR-032 冲突**，见 §7 | 不取 / 待裁决 |
| B6 台词 SRT | 决策记录 §1 明确"不要字幕" | **不取** |
| B7 封面 | 不在产品终点内 | 不取 |

## 4. 三处它比我们强、且我们能直接受益的

### 4.1 画风要三套描述词，不是一套

它的 `STYLE_TOKEN` 分**人物版 / 场景版 / 视频版**，同一个编号派发三套不同的词，
并且写死"三者不得混用"。理由很实在：角色立绘要"真人演员质感"，
场景要"高质量实景空场景"（**空**场景——不能把人画进背景图），
视频要"24fps 电影帧率、轻微手持晃动感"（时间维度的词，画静态图时是噪声）。

我们今天的 `style_profiles` 是**一套词全场通用**。这是一个具体的、可立即修的缺陷：
用同一句"电影级布光"去要一张场景参考图，模型很可能给你画上人。

### 4.2 时代背景决定人种，且必须显式判定

它的 `ERA_TOKEN` 在最开始就从剧本内证据判定国别 + 时代，强制注入角色档案的"国籍/人种"字段，
并且专门写了一条**"表中的中国是判定结果之一，不是兜底默认"**，
理由是"擅自改成中国，会让立绘的人种、服装、发型与场景的建筑、街景、室内陈设全部错位，
且错误会一路传导到分镜"。

我们的 `CharacterSheet` **没有国籍/人种字段**。日系原作跑出来的角色长什么样，
今天完全靠模型自由发挥，而且每次可能不一样。

### 4.3 性格 → 外观的映射表

它给了一张"强势/权力型 → 眉骨立体、眼神锐利、服装整洁有质感"这样的映射表，
外加一套身材词库并规定**必须从词库里选，不许用"匀称""普通"这种模糊词**。
这直接对应我们一致性引擎的目标：**外貌描述必须是可复现的有限词表，不是自由文本**。

我们今天的角色档案字段是自由文本，同一个角色两次生成可能一次"身材匀称"一次"体型适中"，
而这两句话喂给出图模型会得到不同的人。

## 5. 时长引擎：它填上了 ADR-034 缺的那一半

它的公式（`时长与排版铁律.md`）：

```
台词基准秒 = ceil(汉字数 ÷ 4)        只数汉字，不含标点与方括号内的动作描述
动作秒     = 简单 2 / 中等 3~4 / 复杂 5~8 / 空镜 3~4（空镜无对白加成）
最终时长   = 台词基准秒 + 动作秒，向上取整
下限       = 默认 2 秒，情绪特写 4 秒
```

**这正是我们缺的东西。** ADR-034 定的是 `duration_input = max(计划时长, 配音真实时长)`，
但"计划时长"从哪来我们一直没定义——TTS 还没接，分镜阶段又必须知道每镜多长。
这套公式给的就是那个计划值，而且它是**确定性的**（数汉字），符合我们
"时长不让模型填"的既有决定（`agents/schemas.py:415` 的注释原文就是这么写的）。

**注意一个矛盾**：我们的 `StoryboardShot` 注释说"批次由 15 秒规则切分"——
15 秒这个数正是从这套提示词来的（它是 Seedance 的批次上限）。
但 ADR-034 已经把镜内切段降级为待定。这两处需要对齐，见 §7。

## 6. 九宫格：两个独立来源撞上了

它的 C4 是"一次出 3×3 大图，每格是一个关键帧，切格后逐镜走图生视频"。
2026-09-05 的同行代码库调研**独立地**得出了同一条建议（一次出图产出 N+1 个关键帧，
天然同画风同角色，且比逐张出图便宜）。

两个来源指向同一个机制，这是很强的信号。但**我们一次都没实测过万相能不能画出对齐的无边框宫格**，
在验证之前不要写进 ADR。这是 §9 待验证清单的第一条。

## 7. 一处真实冲突：批次 15 秒 vs 每镜一条 MP4

它的 C3「批次版」把**多个镜头合并进一次 15 秒的视频生成**（逐镜累加时长，
即将超过 15 秒就开新批次）。而 ADR-032 定的是**每个镜头一条独立 MP4**。

这两条不能同时成立：一次生成覆盖三个镜头，就没法给每个镜头一条 MP4。

我的判断是**保持 ADR-032 不变**，理由：
1. 每镜一条 MP4 是负责人 2026-09-02 明确拍板的产品终点（决策记录 §1），
   目的是让用户拿到可自行拼接的素材。
2. 批次合并是为了迁就 Seedance 的计费与调用次数，是**成本优化**，不是产品需求。
   在我们还没测出真实废片率之前就为成本改产品形态，顺序反了。
3. ADR-034 已经确认主力模型（wan2.7 2~15 秒、wan3.0 2~30 秒、Seedance 4~15 秒）
   下一个镜通常一次生成就够，批次合并省不下几次调用。

**但它的 15 秒批次规则要留一条记录**：`StoryboardShot` 的注释目前写着"批次由 15 秒规则切分"，
这句话在 ADR-034 之后已经过时，要改。

## 8. 一件必须请负责人定的事：版权

Skill 的 SKILL.md 末尾写着：

> 三份原始提示词版权归 **超级AI客（超哥）** 社群所有 …
> 本 Skill 仅为本地整合封装，未改动其创作逻辑。**对外分发本 Skill 的产出物时请保留作者署名。**

个人本地用是一回事，**把这些提示词烘焙进一个对外收费的 SaaS 的内置 Agent** 是另一回事。
我们的 `agents/builtin/` 是随代码发布的平台内置能力，用户按 Credits 付费使用。

这不是我能替你决定的。三条路：

1. **只取方法不取文本**：用它的**结构性知识**（三套画风词的分工、ERA_TOKEN 判定顺序、
   性格→外观映射的思路、时长公式），但**提示词正文全部自己重写**。
   公式和"角色档案要有国籍字段"这类事实不受版权保护，具体措辞受。**我推荐这条。**
2. **联系作者授权**，在产品内保留署名。
3. **放进 Skill 层而不是内置层**：ADR-020~024 的 Skill 机制本来就是给"第三方生产模板"用的，
   由用户自己上传自己那份。我们不分发，只提供运行它的能力。这条在法律上最干净，
   但 Skill 运行时至今没接线（模块 10 状态：运行时冻结）。

## 9. 建议的执行顺序

**第一波（不依赖任何未决事项，直接可做）**

1. `style_profiles` 拆成人物 / 场景 / 视频三套描述词，`compose_*` 各取各的。
   这一条独立成立——即使不接这份 Skill，"给场景参考图的提示词里带人物质感词"也是个 bug。
2. `CharacterSheet` 补 `nationality` / `ethnicity` 字段，并在 `visual_character.yaml` 里
   规定必须从剧本内证据判定、禁止默认套用本国。
3. 角色外貌字段从自由文本收紧为**有限词表**（身高/体型/体态各一个枚举）。
   这是一致性引擎的地基，`reference_embedding` 至今为空就是因为没有可比的结构化描述。

**第二波（要先定 §8 的版权路线）**

4. 四份 Agent 提示词的增强（screenplay / character / scene / storyboard）。
5. 导演铁律（POV、正反打、大动作拆解、台词特写）与切镜下限进 `visual_storyboard.yaml`。
   **注意**：这会让镜头数显著增加，直接影响每集成本，要先算一遍单集经济性。

**第三波（M2）**

6. 时长引擎落成确定性代码，作为 ADR-034 的"计划时长"。
7. 空间锚点卡填进 `SceneSheet` 已有的 `camera_axis` / `fixed_references`。
8. 九宫格：先实测，再决定。

## 10. 待验证清单（都要花钱，等负责人批准）

1. 万相能否画出对齐、无边框的 3×3 宫格。决定九宫格路线是否成立。
2. wan2.7 是否真的接受 2~15 秒任意时长（ADR-034 的前提，来自二手资料）。
3. 导演铁律全开之后，一集的镜头数与成本变成多少。
