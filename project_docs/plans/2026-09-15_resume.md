# 2026-09-15 接续与验收

## 用户指示

- 历史 provider session 仅作只读参考，不恢复、不修改或删除 transcript。
- 当前工作区为权威，保留未提交修改，在 main 接续。
- Leader 负责写清需求、分派及验收；实施交 Worker，验收失败明确列问题并返工。
- 2026-09-16 用户明确：写代码交给 **Claude Opus**；后续实施不再派 Codex。
- 受本机资源限制，一次一个实施 Worker；独立只读验收可同时进行。

## 本轮范围与任务顺序

Orca Run：`run_ec610b10bfce`。旧 Run/终端不恢复。

1. 提示词返工 `task_6b9e04d4fe2e`：修复成品词外貌、主轴、锚点事实保障；错误文案、旧Key断言、格式门禁。任务书 `orca/tasks/PROMPT_REWORK_0915.md`。
2. CI 修复 `task_309da321a518`：fresh数据库迁移前安装扩展；任务书 `orca/tasks/CI_FIX_0915.md`。
3. Credits 权限 `task_1d6c930922ef`：关闭公开topup、白名单admin-grant、审计、幂等和并发；继承 `RELEASE_CREDITS_0914.md` 契约并补严格参数及并发验收。

每项 Worker 自报后必须独立复核。代码验收与真实模型视觉验收分开记录，不把Mock成功说成真实质量通过。生产部署尚未授权。

## 接续时已独立复核

- 原针对性92测试全部通过，但可执行反例证实成品词缺角色hair/outfit、场景及首帧camera_axis和anchor.description仍被校验器放行，因此后端未通过验收，已派返工。
- Ruff format全树7文件失败，与交接一致。
- CI YAML UTF8解析及9段shell的bash-n通过；fresh PostgreSQL缺vector扩展初始化，需修复后演练。
- 独立API容器原为定时退出，已核验owned标记、label、挂载、隔离配置及640MiB/.75CPU限制后启动。DB/Redis/bucket仍为20260913命名空间，未动生产数据。
- 调查证据 `orca/tasks/CI_REVIEW_0915.report.md`。尚未同步本轮代码或运行新增数据库集成。

## Worker启动记录

Claude启动两次未进入实施：自动启动把权限flag当成输入；自定义启动停在bypass初次提示后退出。两个本轮终端均已关闭，未改产品代码。
改用Codex Terra max（额度脚本当时周使用64%），终端 `term_e2ae609f-154d-4df9-8d69-dde4d24e6e2f`，Dispatch `ctx_5f79248aa643`；已核对实际TUI模型/effort。启动回执不等于开工，另核验ack与执行输出。

2026-09-16 接续：Codex 上次因额度耗尽停止，未交付修复；已关闭该终端。
按用户要求新开 Claude Opus，终端 `term_41f0a8e4-c0d4-4b6d-9e76-6315d84353aa`，
Dispatch `ctx_046b3479d158`，仍执行原提示词Task。实际TUI为 Opus 5 / high / 1M；
先处理首次启动确认再派发，避免任务输入撞上启动菜单。已核对 ack 更新时间与代码调查输出。

## Credits验收补充

- amount严格整数；拒绝bool/float/string强转，note/key空白规范化规则明确。
- 目标组织包括软删除检查；需允许auth service/repository最小存在性查询接口，不跨模块直查models。
- 幂等存储不超160字符；加锁后重查key与payload；并发相同key冲突及不同key增量不丢失，覆盖首次account创建。
- operator外键RESTRICT/NO ACTION，不能CASCADE或SET NULL删改审计。欢迎赠送和旧流水保持NULL。
- 仓库尚未发现不可变流水数据库触发器，不能凭模型注释声称已有数据库级强制保护。
