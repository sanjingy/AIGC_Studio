"""桌面伴随进程：在用户自己的机器上跑官方 CLI，把文本结果回传给服务端。

**只用标准库。** 它要跑在用户的 Windows 桌面上（这台机器上是 Python 3.14），
那里没有、也不该有整套后端依赖——为了跑一个转发器去装 asyncpg + pillow +
cryptography 是荒唐的，而且装不上的那天试点就停了。

安全边界（每一条都是有意的，改之前先读 `project_docs/modules/15_LOCAL_RUNTIME.md`）：

* **只出站**。桌面不监听任何端口，服务端拿不到通往这台机器的路。
* **不是远程 shell**。服务端能影响的只有提示词文本；命令行由本地这份代码
  写死，路径、参数、可执行文件都不来自网络。
* **不碰登录态**。CLI 的订阅登录由 CLI 自己管，这里既不读也不复制
  `auth.json` / keychain，只是启动进程时把它需要的目录留在环境里。
* **不给子进程 API Key**。子环境里的 `ANTHROPIC_API_KEY` / `OPENAI_API_KEY`
  等一律剔除：留着它们，一次订阅额度耗尽就会静默转成按量付费。
"""

__all__: list[str] = []
