"use client";

/**
 * 悬浮 AI 助手占位——Worker D 的任务范围，见需求文档「悬浮 AI 助手」一节。
 * 挂载点在 `apps/web/app/freeflow/layout.tsx`，跨路由常驻，本文件内容
 * 由 Worker D 整体替换，foundation 阶段只保证挂载点存在、编译通过。
 */
export function FloatingAssistant() {
  return null;
}
