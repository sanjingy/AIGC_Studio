import { GlobalPlaceholder } from "@/components/freeflow/global-nav-rail";

// 全局层占位页，分支内未展开（README「全局层」）。
export default function FreeflowTemplatesPage() {
  return <GlobalPlaceholder title="模板" />;
}
