import { GlobalPlaceholder } from "@/components/freeflow/global-nav-rail";

// TODO(Worker A)：03 素材库——一级类型 chip + 二级筛选 + 资产卡片网格。
// 见需求文档「屏幕 03」、REQ-030/031。
export default function FreeflowAssetsPage() {
  return <GlobalPlaceholder title="素材库" />;
}
