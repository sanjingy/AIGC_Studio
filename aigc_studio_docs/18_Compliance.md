# 合规与内容安全

> **状态：占位，待补充。**
>
> 本文档暂不展开，但**必须占住这个位置**，原因是：
> 合规不是可以最后补的功能，其中至少两项会侵入核心链路，事后返工代价很高。

---

## 为什么不能拖到最后

| 项 | 侵入位置 | 事后补的代价 |
|---|---|---|
| AIGC 内容标识（显式水印 + 隐式元数据） | ffmpeg 渲染管线、图片输出 | 已产出的存量内容全部要重新渲染 |
| 生成前 / 生成后内容审核 | Runtime Gateway 中间件 | 需要改动所有 Provider 调用路径 |
| 日志与素材留存 | Asset Service 生命周期策略 | 已删除的数据无法找回 |
| 实名认证 | 用户体系 | 需要回溯已注册用户 |

**建议：M1 阶段就把审核中间件的"空壳 + Mock 实现"接进 Runtime Gateway，
渲染管线预留水印注入点。** 具体规则可以后补，接口位置不能后加。

---

## 待补充清单（TODO）

- [ ] 适用法规梳理与合规义务清单（请法务确认，本文档不作法律意见）
- [ ] 备案事项：ICP、算法备案、生成式 AI 服务上线备案的触发条件与时间线
- [ ] 内容标识方案：显式水印样式 / 隐式元数据字段 / 视频与图片分别如何注入
- [ ] 审核策略：输入 prompt 审核、输出图像审核、输出视频审核、送审服务商选型与成本
- [ ] 审核结果的产品处理：拦截、模糊、人工复核队列、申诉流程
- [ ] 用户实名与未成年人保护
- [ ] 日志与生成内容留存期限
- [ ] 训练数据与用户上传素材的版权责任划分（用户协议）
- [ ] 用户上传 Skill / Workflow 的安全审查（与 `04_SkillSpec.md` 第 3 节联动）
- [ ] 举报与下架流程
- [ ] 审核成本核算（回填 `19_UnitEconomics.md` 第 1.5 节）

---

## 已知的架构预留点

在补齐本文档之前，以下预留点应先实现为可插拔的空实现：

```text
Runtime Gateway
  ├── pre_generation_hook   → ModerationService.check_input()    [Mock]
  └── post_generation_hook  → ModerationService.check_output()   [Mock]

Render Pipeline
  └── watermark_stage       → WatermarkService.apply()           [Mock]

Asset Service
  └── retention_policy      → 可配置留存期                        [默认永久]
```

---

## 变更日志

| 日期 | 变更 |
|---|---|
| 2026-08-16 | 建立占位文档，列出 TODO 与架构预留点 |
